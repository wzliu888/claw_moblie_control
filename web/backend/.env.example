PORT=3000

# MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=your_db_name

# SSH tunnel node — password for the shared Linux user 'cp_shared' on the tunnel server
SSH_SHARED_PASSWORD=your_ssh_shared_password
